import React from 'react';

const SideBarOpenButton = props => (
  <div className = 'side-btn-container'>
    <button type = 'button'>Projects Menu</button>
  </div>
)
export default SideBarOpenButton;
